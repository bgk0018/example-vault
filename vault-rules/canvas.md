---
trigger: glob
globs: **/*.canvas
description: Pointer to the json-canvas skill for editing .canvas files
---
When working with `.canvas` files, invoke the `@json-canvas` skill for the JSON Canvas specification, node types, edge format, validation checklist, and layout guidelines.
